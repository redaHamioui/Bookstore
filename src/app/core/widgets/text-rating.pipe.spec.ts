import { TextRatingPipe } from './text-rating.pipe';

describe('TextRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new TextRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
