import { HtmlRatingPipe } from './html-rating.pipe';

describe('HtmlRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
